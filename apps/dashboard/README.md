# apps/dashboard — Proof-of-Edge (Vite + React)

> Scaffolded in **Phase 7**. Placeholder until then (no `package.json`, so pnpm
> ignores it).

The Proof-of-Edge dashboard: positions/edges table, settlement cards with Solana
Explorer links + Merkle verdict, running P&L, a live event stream, and the
**RPC Health** panel sourced from solana-resilience-kit telemetry (§11b). Static
Vite build deployable to Cloudflare Pages.
