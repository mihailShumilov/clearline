/**
 * @clearline/agent — the autonomous decision/settlement orchestration.
 * Ingest tick → decide (core) → open position → settle (chain) on completion.
 * Runs inside a Cloudflare Durable Object + Cron Trigger; D1-persisted.
 * Implemented in Phase 5.
 */
export const AGENT_PLACEHOLDER = true as const;
