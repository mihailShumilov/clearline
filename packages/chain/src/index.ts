/**
 * @clearline/chain — the project's ONLY RPC path (§11b).
 * Wraps solana-resilience-kit (ResilientRpcPool, TransactionSender, OtelMetrics,
 * ClusterDetector) over ≥2–3 devnet endpoints. Implemented in Phase 2.
 */
export const CHAIN_PLACEHOLDER = true as const;
